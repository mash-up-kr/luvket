import Theme from '../../models/Theme';

const theme: Theme = {
  palette: {
    gray: {
      100: '#d8d8d8',
    },
  },
  typography: {
    button: {
      fontSize: '14px',
    },
  },
};

export default theme;
